cache:
  paths:
    - .m2/repository

build:
  image: maven:3-jdk-8
  script: mvn clean package
  artifacts:
    name: "Zuul"
    paths:
      - target/*.jar

pages:
  image: maven:3-jdk-8
  script:
    - sh doc.sh
    - mkdir .public
    - cp -r progdoc .public/
    - cp -r userdoc .public/
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master